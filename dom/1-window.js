// WINDOW METHODS, OBJECTS, PROPERTIES

let val;

val = window.outerHeight;
val = window.outerWidth;
val = window.innerWidth;

val = window.scrollY;

// location Object

val = window.location;
val = window.location.hostname;
val = window.search;

// Redirect
// window.location.href = 'http://js-learning.test/'
// window.location.reload();
val = window.history;
// window.history.go(-1)
val = window.history.length;

// Navigator object
val = window.navigator;

console.log(val);